# Lootem's Dev Template

* Develop in containers with VSCode devcontainers:
    * `.devcontainer/Dockerfile`
* Install and configure formatting extensions such as `black` and `isort`:
    * `.devcontainer/devcontainer.json`
    * `.isort.cfg`
* Define code editor settings:
    * `.vscode/settings.json`
    * `.editorconfig`